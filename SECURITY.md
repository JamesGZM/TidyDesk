# 0.4.8 权限引导撤回（2026-09-06）

## 风险

0.4.8 从可由同一用户进程写入的安装目录启动未签名的 TidyDeskIcons.exe，并请求 UAC 提权。另一同用户进程可能在预期授权前替换该文件；用户若批准替换后的程序，它可能获得高权限。该版本还会复制助手到 Program Files 并注册自动 LocalSystem 服务，可能使这种执行持久化。

服务安装后的 ACL、固定请求及用户检查没有认证提权前的可执行文件。此前的受限令牌测试仅覆盖受保护目的文件，不能证明引导安全。没有证据表明该风险已被利用；是否存在服务也不能据此推断被入侵。

## 0.4.9 修复范围

- 删除所有产品 runas 调用、服务创建和服务安装入口。
- 不再把 TidyDeskIcons.exe 放入安装包或便携包；该构建目标只运行隔离的注册表及图标测试，不接受旧操作命令。
- 停用一次授权开关和系统箭头新修改，保留普通刷新。
- 旧管道客户端只允许恢复及停用；不接受新的隐藏请求，不执行任意路径或命令。
- 为旧安装保留显式恢复及清理入口；不会自动重装、提权或重启 Explorer。
- 撤回本次发布的三个 0.4.8 Actions 安装产物，保留提交和日志用于审计。

较早版本的按需箭头提权也不能被视为来源已认证；0.4.9 一并移除产品内的这种调用。未提供代码签名之前，不宣称拥有可信的自动提权安装链。

## 检查

CI 检查提权及服务引导 API 不存在、旧安装命令被拒绝、测试助手不进入最终包。源代码及构建检查不替代用户电脑上的旧服务清理；不会根据 CI 结果声称真实 UAC 交互已验证。
