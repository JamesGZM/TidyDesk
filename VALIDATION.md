# LiteTaskbar 0.3.0 验证记录

2026-09-05，Windows 11 25H2，26200.9168，x64。

## 窗口规则修复

原规则仅判断前台窗口，普通大小的 Codex 盖在最大化浏览器上时会恢复透明，不符合用户期望。现改为扫描当前桌面未最小化、未隐藏的顶层应用窗口：存在最大化窗口就使用系统背景。被其他窗口遮挡仍计入，其他虚拟桌面中被 DWM 隐藏的窗口排除。窗口事件合并后检查，状态不变不重复发送，无空闲轮询。

原生回归测试覆盖最大化窗口被普通窗口/弹窗盖住、全部还原或最小化、隐藏最大化窗口、无边框窗口铺满工作区。测试仅枚举测试进程创建的窗口，不受 CI 中其他程序窗口影响。

## 之前已验证

- 原生设置页、应用图标显示正常。
- 自启开关完成界面开启、保存、关闭；重开设置显示关闭。未测试实际注销或重启登录。
- 资源管理器重启后，0.3.0 连接成功，用户确认透明效果正常。
- Windows XAML 服务曾在反复连接后返回 0x8000FFFF；重启资源管理器恢复。错误码现保留，不再随后误报“未找到背景”。连接重试兼容性仍属实验范围。

## 资源与限制

0.2.1 历史短时测试主进程约 9 MiB；0.3.0 打开设置后主进程会加载原生控件，不能沿用旧版数字作为本版保证。主进程内存不包含 Explorer 中的后端和 DWM。

未全面验证多显示器、不同虚拟桌面、睡眠唤醒、长时间运行。多显示器当前共用规则：任一符合条件窗口会影响全部已连接任务栏。此版本仍是实验版。

## 本次构建与实机记录

构建：https://github.com/JamesGZM/LiteTaskbar/actions/runs/33966851559
源代码：8466dc680760ec1448863f1cbefe07337ef56b57。MSVC 编译和全部四项 CTest 通过。

新版运行状态：maximized_rule=1，requested_opacity=0，effective_opacity=100，background_elements=1，hresult=0。说明后台最大化规则已发出系统背景指令；视觉结果仍需用户确认。

切换测试版本时，系统记录 Explorer 在 Windows.UI.Xaml.dll 中发生 0xc000027b 异常并自动重启，随后新进程连接成功。这一连接生命周期问题尚未修复，不能将此包视为稳定版本。不建议反复退出和重新连接来测试。
