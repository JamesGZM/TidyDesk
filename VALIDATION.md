# LiteTaskbar 0.2.1 验证记录

日期：2026-09-05。系统：Windows 11 25H2，26200.9168，x64，8 个逻辑处理器。

## 已完成

- GitHub Actions Windows 云端编译通过，MSVC /W4 /WX。
- 命令行参数测试、DLL COM 工厂初始化测试通过；本机 DLL 自检通过。
- 首版实机：用户确认任务栏背景透明，图标与点击正常。
- 优化版实机：后端成功修改 1 个 Taskbar.TaskbarBackground 元素；连接辅助进程退出。
- 用 LiteTaskbarStop.exe 正常退出后，状态报告 restored、背景元素计数归零、主进程结束。
- 再次启动后重新成功应用透明效果，Explorer 进程未重启。
- 常驻主程序模块列表中未发现 Windows.UI.Xaml 或测试机的输入法模块。

## 采样

使用 Get-Process 两次读取累计 CPU 时间，以实际间隔和逻辑处理器数换算总 CPU 百分比。内存为采样末端的工作集和 PrivateMemorySize64，单位 MiB（1,048,576 字节），不是峰值。工作集包含共享页，私有内存是私有提交量，两者不应相加。

| 区间 | 程序 | 间隔（秒） | 平均总 CPU | 工作集（MiB） | 私有内存（MiB） |
| --- | --- | ---: | ---: | ---: | ---: |
| 0.2.0 首版运行 | LiteTaskbar | 30.30 | 0.0000% | 47.62 | 11.42 |
| 优化版启动前 | Explorer | 30.29 | 约 0.11% | 409.55 | 390.92 |
| 0.2.1 优化版运行 | LiteTaskbar | 45.242 | 0.0000% | 9.01 | 1.47 |
| 同一运行区间 | Explorer | 45.242 | 0.1770% | 359.52 | 391.44 |
| 同一运行区间 | DWM | 45.242 | 无法读取 | 140.72 | 294.50 |

0% 表示采样期间进程累计 CPU 时间未发生可测增量，不代表永远不使用 CPU。运行期间仍有正常桌面活动，这不是严格隔离的性能基准。

## 解释与限制

内存优化将 XAML 连接移至短时辅助进程，并关闭无输入框托盘线程自身的输入法支持。没有使用清空工作集或强制定期回收来制造低内存读数。

主进程数值不包含 Explorer 内的后端和 DWM 开销。优化版测试前，旧版 DLL 和 XAML 运行时已加载过，因此 Explorer 的前后差值不能当作冷启动总开销。尚未隔离测量后端总内存，也未与 TranslucentTB 进行同条件对照，不能声称整体一定比它更省资源。

未验证：多显示器、睡眠唤醒、Explorer 重启、数小时持续运行、其他 Windows 版本、与其他任务栏美化工具同时运行。主进程异常结束后的恢复已实现，但本轮没有完成真实桌面的强制结束测试。

## 构建来源

源代码提交：a730447a400fedee7c0ebc247d83c6f3d2e6adb0（之后仅补充文档）。

云端构建：https://github.com/JamesGZM/LiteTaskbar/actions/runs/33965086807

原始构建 ZIP SHA-256：`1df85eb499953e351e5d847ca9d58f316dba6debaa90532ab2cbebc0066a0783`。

发布包补充了本文及使用说明，ZIP 校验值另附；内部 EXE/DLL 与上述构建一致，`SHA256SUMS.txt` 可核对。
