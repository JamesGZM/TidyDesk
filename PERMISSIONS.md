# TidyDesk 0.4.12 权限说明

任务栏、收纳格和普通设置以当前用户权限运行。快捷方式箭头属于系统级覆盖设置，影响本机用户，只有点击“应用”时才请求管理员授权。

## 箭头设置的执行范围

- 仅启动 GetSystemDirectoryW 返回的系统目录下的 Windows reg.exe；工作目录也固定为该系统目录。
- 参数只生成 Shell Icons 的 29 值的 add/delete 操作；不接受其他键、程序路径、脚本或命令。
- 使用 ShellExecuteEx 直接传参，不经过 cmd、PowerShell 或临时脚本。每次修改需要独立的 Windows 授权，没有“一次授权”服务。
- 设置前读取原值并备份到 HKCU/Software/TidyDesk/ArrowSettingBackup；保留原值是否存在及 REG_SZ/REG_EXPAND_SZ 类型。其他数据类型拒绝覆盖。
- 兼容读取旧 HKLM/Software/TidyDesk/ArrowBackup。备份损坏或发现不相关的后续修改时停止，保留备份。
- 完成后读取实际注册表值确认。取消授权不写入系统项；可以重试。

这移除了 0.4.8 中从用户可写目录运行未签名提权助手的路径。它不是对整个应用的签名保证，也不能阻止管理员篡改操作系统。注册表写入与其他程序的并发修改不构成原子比较交换。

## 图标与刷新

箭头是单独的覆盖层，不更改快捷方式目标和软件原图标。透明 ICO 保存在当前用户 LocalAppData/TidyDesk/Assets/shortcut-overlay-v4.ico，卸载保留该文件和备份，避免注册表指向已删除资源。

新资源同时提供近透明 alpha 和全透明 AND 遮罩，测试覆盖不同尺寸、背景与兼容遮罩。系统注册表读取成功不代表 Explorer 缓存已经更新。应用后可刷新图标；无效时需用户重新登录，不自动重启 Explorer、不强行删除图标缓存。

卸载前如需恢复系统箭头，先关闭隐藏开关并应用。卸载不会静默提权修改系统设置。若之前安装的辅助服务仍可用，图标页另有恢复原设置入口用于原有组件清理。

0.4.8 历史风险与撤回记录见 SECURITY.md。
