# 权限功能状态（0.4.9）

0.4.8 的“授权一次”实现已撤回。0.4.9 不提供重新启用开关，不提权启动用户目录中的助手，不创建服务或高权限计划任务，也不保存管理员凭据。

任务栏、收纳格与设置继续以普通用户权限运行。系统箭头的新修改暂停；已有外观不会在升级时静默改动。

已经启用旧助手：在“常规”点击“恢复并停用旧助手”，确认后通过原有受限管道恢复备份并撤销服务，随后请求普通图标刷新。不会自动重启 Explorer。
未安装旧助手：此按钮不可用，无需操作。
旧助手停止、属于其他用户或无法核实时：显示未完成，不回退到 runas、不重装服务，请由 Windows 管理员检查。未经确认不强行改写原备份。

卸载先尝试同样的旧服务清理，失败则保留安装和备份。小型图标资源保留，避免 Explorer 的缓存引用失效。若只有历史箭头设置而无助手，卸载不会提权清除它；这需要管理员另行检查原备份。

此修复移除不安全的产品提权路径，不代表程序获得代码签名，也不代表已经安装的旧文件自动被清理。详见 SECURITY.md。
